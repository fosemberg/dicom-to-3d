{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "run-p start-back start-front",
    "start2": "concurrently \"npm run start-back\" \"npm run start-front\"",
    "start-back": "cd back && node -r dotenv/config ./build/index.js dotenv_config_path=back/env/default/.env",
    "start-front": "node ./node_modules/serve/bin/serve.js -s front/build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.0.0",
    "npm-run-all": "^4.1.5",
    "serve": "^11.2.0"
  },
  "dependencies": {
    "dotenv": "^8.2.0"
  }
}
