export const DB_DIR = `${__dirname}/../../db`
export const DB_NAME = 'db.json';
export const DB_FULL_PATH = `${DB_DIR}/${DB_NAME}`;
export const STATIC_DIR = `${__dirname}/../public`;

export enum MESSAGE {
  CONNECTED = 'successfully conected'
}
